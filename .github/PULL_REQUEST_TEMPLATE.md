## Summary

- What changed?
- Why was this needed?

## Layer

Mark the main layer touched by this PR:

- [ ] Repo contract (`CLAUDE.md`, `.claude/`, `harness/specs/`, `harness/templates/`)
- [ ] Portable runtime (`harness/plugin/runtime/`)
- [ ] Change lifecycle commands (`harness/bin/`)
- [ ] Java reference service (`reference-service/`)
- [ ] Validation / hooks (`hooks/`)

## Validation

List the commands you ran and the actual outcome.

```bash
# paste commands here
```

## Change Assets

If this is a non-trivial change, link the related change asset path:

- `harness/changes/<change-id>/...`

## Notes / Follow-ups

- What is intentionally out of scope?
- What remains for the next iteration?
