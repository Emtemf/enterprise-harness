# Harness behavioral eval — 2026-08-21

## Environment

- Claude Code: 2.1.237
- Model: Sonnet
- Plugin source: current checkout via `--plugin-dir`
- Mode: print, plan permission, no tools, no session persistence
- Purpose: exercise Skill instructions only; no change creation or file mutation

## Results

Each run used:

```bash
claude -p --plugin-dir <checkout> --tools "" --permission-mode plan \
  --no-session-persistence --model sonnet --max-budget-usd 0.50 '<prompt>'
```

### Breadth before depth — PASS

Input state: active components A=1, B=2, C=2; two consecutive Decision questions had already targeted A.

Prompt:

```text
这是一次只读 behavioral eval，不创建 change、不调用工具、不写文件。已有三个 active components：
A 的最低分为 1，B=2，C=2；我已经连续回答了两个关于 A 的 Decision 问题。请严格按
harness skill，仅输出你现在会展示给用户的状态摘要和下一轮问题。
```

Observed behavior:

- activated the two-question breadth guard;
- selected B instead of asking a third A question;
- produced one user question with a recommended first option and free input;
- noted that continuing A would require dependency evidence.

Observed output excerpt:

```text
Round Ledger: 2 consecutive Decision questions on A → circuit breaker engaged.
Weakest Frontier: Tie between B (score 2) and C (score 2).
Next Question (Component B ...)
```

### Weakest / highest-risk with pending Fact — PASS

Input state: Cancel API was fully ready; Refund had a high-risk Goal Decision, a lower-scored medium-risk
Acceptance Decision, and a pending Constraints Fact.

Prompt:

```text
Round 0 已确认两个组件：Cancel API 与 Refund。Cancel API 五维都是 4；Refund Goal=3
（原路退款还是余额，Decision，high risk），Scope=4，Constraints=3（退款超时，Fact，
ResearchPacket pending），Acceptance=2（成功与失败如何验收，Decision，medium risk），Context=4。
请仅输出状态摘要和下一轮问题。
```

Observed behavior:

- selected the high-risk Refund Goal Decision;
- kept the pending Fact on the research lane and did not ask the user for it;
- explained why Acceptance depended on the selected Decision;
- asked exactly one question with three mutually exclusive options, recommendation first, and free input.

Observed output excerpt:

```text
Refund · Goal=3 — Decision，high risk，本轮询问
Refund · Constraints=3 — Fact，不问用户；ResearchPacket pending
本轮问题（Refund · Goal）— 退款资金走哪条路径？
A. 原路退回（推荐）
B. 退到账户余额
C. 混合策略
```

### Fast Path zero-question confirmation — PASS

Input state: fact agent confirmation was supplied; one component had all five scores at 4; the original
request explicitly listed scope, non-goals, acceptance, constraint, and authorization to proceed.

Prompt:

```text
模拟 fact agent 已确认路径与现有约定。用户要求在 OrderService.java 增加 cancelPending(id)，
只有 PENDING 可取消，否则返回 ORDER_NOT_PENDING；补单元测试；不改 API、数据库或其他模块；
并确认授权仅按这个完整 scope 推进。一个 component 五维均为 4，无 high-risk assumption。
请仅输出此时用户会看到的状态和下一动作。
```

Observed behavior:

- treated the original request as the recorded scope confirmation source;
- asked zero additional questions;
- displayed provisional topology, scores, non-goals, and empty frontier;
- retained the same finalizer, independent review, CompletionProof, and transition gates as normal Clarify.

Observed output excerpt:

```text
Scope confirmation source：用户原始请求 — 已明确列出完整 scope/non-goals 并显式授权按此推进
→ 0 个额外问题
Frontier：无 weakest frontier
下一动作：创建 clarify.confirmed handoff → finalizer → independent reviewer → fresh evidence gate
```

## Remaining coverage

The six cases in `evals.json` remain the reusable suite. Before release across multiple Claude model tiers,
repeat these prompts on the supported Haiku, Sonnet, and Opus versions and record any model-specific failure.
