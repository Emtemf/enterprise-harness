# Tasks

- `task-domain-policy`：真实 RED/GREEN/REFACTOR，冻结定向 Maven 测试 argv。
- `task-application-transaction`：覆盖并发与幂等。
- `task-http-contract`：覆盖成功、404、409 与 OpenAPI。
- `task-verify`：执行 `./mvnw verify`，消费独立 reviewer verdict。
