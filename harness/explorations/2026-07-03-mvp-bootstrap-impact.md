# MVP Bootstrap Impact Analysis

## Scope

Bootstrap a local inspectable MVP for the enterprise harness blueprint.

## Expected impact areas

- root governance files (`CLAUDE.md`, `rules/`, `agents/`, `hooks/`)
- harness artifact directories and example records
- `.claude` project settings for local hook wiring
- `reference-service/` Spring Boot sample application
- `reference-service/openapi/order-service.yaml`

## Why this artifact exists

This exploration file demonstrates the required `explorations/` naming and archival pattern for the harness model.
